const letters = ["a", "b", "c"];

const newArray = letters.map((letter) => letter.toUpperCase());
// const newArray = [];

// for (let letter of letters) {
//   newArray.push(letter.toUpperCase());
// }
console.log(newArray);
